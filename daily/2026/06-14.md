# 2026-06-14

## Today

### Backend

* 完成直播系统 Room 生命周期流转逻辑（Create / Join / Leave / Close）
* 完成直播相关基础 API（HTTP + WebSocket）
* 完成 Lobby 房间列表接口
* 完成房主退出自动关闭房间逻辑
* 排查并修复房间关闭后导致的脏数据问题
* 明确房间状态以服务端为准，客户端仅负责展示状态

### Frontend

* 接入直播相关 API
* 完成 LobbyScreen 与 CallScreen 基础 UI 搭建
* 梳理房间成员同步方案，决定采用服务端推送房间快照（Snapshot）模式

---

## Notes

* SkipList 中参与排序的字段发生变化时，需要同步更新索引，否则可能导致索引与实际数据不一致。
* 数据结构中的排序键应尽量避免被外部直接修改，后续可以考虑通过统一 Update 接口维护索引一致性。
* RTC 房间状态应由服务端统一维护，客户端不负责成员增删逻辑，仅接收并覆盖最新状态。

---

## Tomorrow

* 完成房间创建与加入流程联调
* 接入房间成员实时同步
